# Test config for the dedup IT: Hibernate creates the table in the throwaway
# Postgres (public schema), Flyway disabled to keep the slice focused.
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: public
  flyway:
    enabled: false
