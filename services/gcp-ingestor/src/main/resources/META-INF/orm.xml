<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="https://jakarta.ee/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm https://jakarta.ee/xml/ns/persistence/orm/orm_3_0.xsd"
                 version="3.0">

    <!--
      Pin the SHARED outbox table to `public` for this service (Debezium tails
      public.outbox_event). Without this it would default to this service's
      hibernate.default_schema ("gcp_ingestion"). Identical override to
      aws-ingestor's orm.xml — done here, not in the locked outbox-starter,
      because where the shared outbox physically lives is the adopter's choice.
    -->
    <entity class="io.finflow.outbox.OutboxEvent">
        <table name="outbox_event" schema="public"/>
    </entity>

</entity-mappings>
