<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="https://jakarta.ee/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm https://jakarta.ee/xml/ns/persistence/orm/orm_3_0.xsd"
                 version="3.0">
    <!--
      Pin the SHARED outbox table to `public` (Debezium tails public.outbox_event).
      FIFTH byte-identical copy of this override; the cue-to-refactor has become
      a chorus. Deferred only because the outbox-starter contract is currently locked.
    -->
    <entity class="io.finflow.outbox.OutboxEvent">
        <table name="outbox_event" schema="public"/>
    </entity>
</entity-mappings>
