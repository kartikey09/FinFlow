<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="https://jakarta.ee/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm https://jakarta.ee/xml/ns/persistence/orm/orm_3_0.xsd"
                 version="3.0">
    <!--
      OutboxEvent lives in the public schema. Debezium tails public.outbox_event.
      This override moves the entity there regardless of the consuming service's
      hibernate.default_schema, so no service needs its own orm.xml anymore.

      Previously each of 7 services shipped a byte-identical copy of this file.
      Day 20 folded it here.
    -->
    <entity class="io.finflow.outbox.OutboxEvent">
        <table name="outbox_event" schema="public"/>
    </entity>
</entity-mappings>
